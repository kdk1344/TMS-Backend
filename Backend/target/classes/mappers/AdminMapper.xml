<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- user-mapper.xml -->
<mapper namespace="com.tms.backend.mapper.AdminMapper">
    <insert id="insert" parameterType="User">
		INSERT INTO users (USER_ID, USER_NAME, PASSWORD, AUTHORITY_CODE, AUTHORITY_NAME)
		VALUES(#{userID}, #{username}, #{password}, 
		CASE 
            WHEN #{roleName} = 'type1' THEN 1
            WHEN #{roleName} = 'type2' THEN 2
            WHEN #{roleName} = 'type3' THEN 3
            ELSE 0
        END,
        #{roleName})
	</insert>
	<insert id="insertUser" parameterType="User">
        INSERT INTO users (USER_ID, USER_NAME, PASSWORD, AUTHORITY_CODE, AUTHORITY_NAME)
        VALUES (#{userID}, #{username}, #{password},#{roleCode}, #{roleName})
    </insert>
    
    <update id="updateUser" parameterType="com.tms.backend.vo.User">
        UPDATE users
        SET UserName = #{user.username},
            Password = #{user.password},
            RoleName = #{user.roleName},
            RoleCode = CASE 
                          WHEN #{user.roleName} = 'type1' THEN 1
                          WHEN #{user.roleName} = 'type2' THEN 2
                          WHEN #{user.roleName} = 'type3' THEN 3
                          ELSE 0
                      END
        WHERE UserID = #{user.userID}
    </update>
    
    <delete id="deleteUser" parameterType="String">
        DELETE FROM users WHERE USER_ID = #{userID}
    </delete>
    
    <select id="findAll" resultType="User" parameterType="Criteria">
        SELECT USER_ID as userId, USER_NAME as userName, AUTHORITY_NAME as roleName
        FROM users
        LIMIT #{pageStart}, #{perPageNum}
    </select>

    <select id="countUsers" resultType="int">
        SELECT COUNT(*) FROM users
    </select>
	
</mapper>